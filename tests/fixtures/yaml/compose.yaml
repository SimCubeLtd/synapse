version: "3.9"

x-common: &common
  restart: unless-stopped

services:
  api:
    <<: *common
    image: synapse-api:latest
    ports:
      - "8080:8080"
  worker:
    <<: *common
    image: synapse-worker:latest

volumes:
  data: {}
